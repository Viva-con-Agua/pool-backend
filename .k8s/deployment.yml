apiVersion: apps/v1
kind: Deployment
metadata:
  name: pool-user
  labels: &labels
    app.kubernetes.io/name: pool-user
    app.kubernetes.io/component: component-explorer
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      containers:
        - name: pool-user
          image: ghcr.io/viva-con-agua/pool-user
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: pool-user
          ports:
            - name: http
              containerPort: 1323
          readinessProbe:
            httpGet:
              path: /healthz
              port: 1323
          resources:
            limits:
              memory: 400Mi
              cpu: 100m # 10 milli-cores = 1% of a core
