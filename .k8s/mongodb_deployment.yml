apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels: &labels
    app.kubernetes.io/name: pool-user-db
    app.kubernetes.io/component: component-explorer
  name: pool-user-db
spec:
  replicas: 1
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      containers:
        - image: mongo
          name: pool-user-db
          args: ["--dbpath", "/data/db"]
          volumeMounts:
            - name: "pool-user-db-storage"
              mountPath: "/data/db"
      volumes:
        - name: "pool-user-db-storage"
          persistentVolumeClaim:
            claimName: "pool-user-db-storage"
